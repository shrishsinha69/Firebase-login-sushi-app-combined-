name: morefirebase
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^3.2.0
  firebase_auth: ^5.1.2
  cloud_firestore: ^5.1.0
  liquid_swipe: ^3.1.0
  smooth_page_indicator: ^1.2.0
  google_fonts: ^6.2.1
  get: ^4.6.6
  shared_preferences: ^2.2.3
  provider: ^6.1.2
#  google_fonts: ^6.2.1

#  animated_smooth_indicator: ^0.4.1+3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - images/

